# 2026-06-02

## Token Cost Optimization

- Vincent asked in `#manager-hq` how to better use API token cost to optimize effectiveness while keeping cost minimal.
- Added `COST_POLICY.md` to the manager-bot workspace.
- Linked `COST_POLICY.md` from `AGENTS.md` so future startup context includes the policy pointer.
- Updated `ROUTING.md` with token/cost guardrails and cost controls for specialist handoffs.
- Updated `HEARTBEAT.md` with cost discipline for batched low-noise heartbeat checks.
- Promoted a sanitized durable decision note to the shared Agent Memory Vault: `Decisions/2026-06-02 API Token Cost Policy.md`.
- Runtime config pass: changed `/home/vin/.openclaw/openclaw.json` default model from `openai/gpt-5.5` to `openai/gpt-5.4-mini`, kept `openai/gpt-5.5` as fallback and `GPT_FULL`, added `GPT_CHEAP` alias for `openai/gpt-5.4-nano`, and `REASONING_CHEAP` alias for `openai/o4-mini`. Validation passed. Backup: `/home/vin/.openclaw/openclaw.json.bak-cost-policy-20260602T055715Z`.
- Routed gateway restart/reload to `#admin-desk` because service restarts belong to admin-bot. OpenClaw reported the config change needs gateway restart to apply.
