# 2026-05-22

- Vincent asked for recommendations on improving use of agents/channels and whether more agents were needed.
- Assessment: current five-agent setup is enough; underuse was mainly procedural, especially keeping FreeCAD/garage-shelving work in `#manager-hq` instead of routing to `#modeling-studio`.
- Applied routing improvements: updated `SOUL.md`, added `ROUTING.md`, and expanded `HEARTBEAT.md` with multi-agent coordination checks.
- Vincent then requested a new cybersecurity agent based on an attached task list. Created `security-bot` / `#security-lab` as a defensive, scope-bound security lane and installed user-local security tools where root was not required.
- Security privilege decision: prefer routing privileged setup through `#admin-desk` with preview and confirmation. Keep `security-bot` unprivileged by default. Recommended rootless `podman` sandboxing for security workflows rather than adding `vin` to the Docker group, because Docker group access is effectively root-equivalent. Confirmation phrase proposed: `CONFIRM SECURITY SANDBOX ROOTLESS PODMAN`.
