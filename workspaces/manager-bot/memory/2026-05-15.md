# 2026-05-15

- OpenClaw daily maintenance failure root cause: the maintenance script ran a strict post-restart health gate immediately after restarting the gateway. Telegram/Discord were still inside startup/channel grace, so health falsely failed, triggered recovery reinstall, then the second health check ran before the recovered gateway was listening and the oneshot exited 5. Fixed by adding a bounded retry loop around post-update and post-recovery health gates in `/home/vin/.openclaw/workspace/scripts/openclaw-maintenance.sh`. Verified with `bash -n`, a direct `wait_for_health_gate` smoke test, and `openclaw health --json`.

- 2026-05-15 07:57 UTC — In #manager-hq, gilamesh asked: “add this to memory.” No specific content was provided beyond the request itself.
