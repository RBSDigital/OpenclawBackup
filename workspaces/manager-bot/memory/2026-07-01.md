- Installed the OpenClaw security skills `security-best-practices`, `security-threat-model`, and `security-ownership-map` for the manager-bot workspace.
- Ran a security audit of OpenClaw config/state: no critical findings, but plaintext credentials remain in `~/.openclaw/openclaw.json` and agent SQLite state, `gateway.controlUi.allowInsecureAuth=true` is enabled, and `gateway.trustedProxies` is empty.
- Added a recurring security-audit step to `/home/vin/.openclaw/workspace/scripts/openclaw-maintenance.sh` so the daily maintenance report now includes `openclaw security audit --deep --json` and `openclaw secrets audit --json`.
