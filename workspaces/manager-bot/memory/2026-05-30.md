# 2026-05-30

## Agent Skill Assignment

Vincent asked in `#manager-hq` to ensure newly added OpenClaw skills were assigned to the correct agents. Installed local skills into specialist workspaces by lane and verified all six agents reported `Missing requirements: 0`. Installed `yt-dlp` user-locally with `uv tool install yt-dlp` so `youtube-watcher` could run.

## Agent Memory Vault

Vincent asked whether Obsidian was being used effectively for persistent memory and recall, then asked to set up the recommended system.

Created shared sanitized Markdown vault at `/home/vin/ObsidianVaults/AdaKTVault/06_system/agent-memory-vault` with index, privacy boundaries, promotion rules, lane folders, templates, and decision records. Updated all agent workspaces to consult and write to the vault within their lane boundaries while keeping private `MEMORY.md` content isolated.

Obsidian skill status: bundled OpenClaw `obsidian` skill still requires the official `obsidian` CLI binary on `PATH`. Current host appears headless with no `DISPLAY`, so the vault is Markdown-first for now and can become Obsidian-native once the official desktop CLI is registered.

Correction: Vincent already had an Obsidian vault for agents at `/home/vin/ObsidianVaults/AdaKTVault`. Moved the agent memory vault into the existing vault under `/home/vin/ObsidianVaults/AdaKTVault/06_system/agent-memory-vault`, updated all agent workspace references, and repointed the qmd `ada_kt_vault` collection to the live vault path. The old `/home/vin/.openclaw/agent-memory-vault` is now only a non-authoritative staging fallback.

## Memory Flush Constraint

During pre-compaction memory flushes, durable memories for this workspace must be appended only to `memory/2026-05-30.md`. Do not create timestamped variant files such as `2026-05-30-HHMM.md`, and treat bootstrap/reference files like `MEMORY.md`, `DREAMS.md`, `SOUL.md`, `TOOLS.md`, and `AGENTS.md` as read-only for the flush.
