# 2026-06-08

## OpenClaw Auth Follow-Up

- Re-ran `openclaw secrets reload` successfully; runtime snapshot swap completed with no warnings.
- Current audit still reports plaintext credentials in `~/.openclaw/openclaw.json` and agent `auth-profiles.json` files, plus two legacy OpenAI OAuth residues.
- The remaining work is credential migration / SecretRef cleanup, which should stay on the admin path rather than hand-editing secrets directly.
