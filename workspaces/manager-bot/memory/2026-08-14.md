# 2026-08-14

- Fixed the OpenClaw GitHub backup failure from `2026-08-14T04:30:28Z`.
- Root cause: the redaction scan in `/home/vin/.openclaw/workspace/scripts/openclaw-github-backup.sh` called `os.path.getsize()` on staged files without handling `FileNotFoundError` in the second pass, so a disappearing staging artifact aborted the whole backup with `secret scan/redaction failed`.
- Patch: treat `FileNotFoundError` and `OSError` as skip conditions in both redaction scan passes, matching the existing resilience goal of ignoring transient staging churn.
- Verification: `bash -n /home/vin/.openclaw/workspace/scripts/openclaw-github-backup.sh` passed after the change.
