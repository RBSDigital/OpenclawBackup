- Added a daily OpenClaw agent utilisation and efficiency report routine at 05:00 UTC, after maintenance (04:00) and GitHub backup (04:30). Files: `/home/vin/.openclaw/workspace/scripts/openclaw-agent-utilisation-report.sh`, `~/.config/systemd/user/openclaw-agent-utilisation-report.service`, `~/.config/systemd/user/openclaw-agent-utilisation-report.timer`, and matching `gog` service override. The first verified email report was sent to `vrbs940054@gmail.com` at 2026-05-26 09:08 UTC.
- Initial utilisation report findings: OpenClaw health and cron list fail against the gateway transport, backup also failed at `openclaw cron list`, `~/.openclaw` permissions are 775, and several specialist workspaces lack fresh daily memory. Route gateway/scheduler and backup/maintenance report coverage to ops-bot; route permissions/secret-hardening preview to admin-bot.
- Root cause for the health/cron failure: CLI defaulted to `ws://127.0.0.1:18789`, but the gateway listens on tailnet `100.113.110.120:18789`; explicit `OPENCLAW_GATEWAY_URL` plus gateway token made `openclaw health --json` and `openclaw cron list --json` pass. Patched maintenance, GitHub backup, and utilisation scripts to derive the listening tailnet URL locally. Backup now captures OpenClaw diagnostic command failures as warning artifacts instead of aborting before the snapshot. Scheduled a one-shot manager-bot cron follow-up for 2026-05-27 05:20 UTC to verify maintenance, backup, and utilisation report results after timers fire.
